system_relation:
  codex_mode: direct | mapped
  authority_surface:
  field_surface:
  registry_surface:
  execution_surface:
  codex_mode:
    direct:
      description: "Directly use the system relation as defined in the system registry."
    mapped:
      description: "Map the system relation to a different structure or format for specific use cases."