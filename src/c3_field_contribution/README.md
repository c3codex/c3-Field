# c2ME_env formation shell

Route: `/c2` (c3field renderer only). Encounter: `c2_contribution_shell`.
This package is inert formation material, not c2 custody or elevation.

Edit `c2Environment.ts` for the formation configuration and seam descriptions,
`C2EnvironmentShell.tsx` for encounter layout, and `c2Environment.css` for scoped styling.
All nine major interfaces are represented. No input field, submit handler,
network request, persistence adapter, Current resolver or wallet interface is connected.
Even a supplied eligibility reference returns held; a browser value cannot release eligibility.

Existing database references (read-only catalog inspection):
- `public.c3_environment`: reuse for future governed environment binding.
- `public.register_and_persist_c1_relationship(p_relationship_key text, p_env_key text, p_metadata jsonb)`:
  existing prerequisite producer, not callable from this package.

No exact c2 environment row was found by env_key/name matching c2/contribution;
no public RPC with c2/initiative in its name was found. This is a narrow lookup,
not proof that formed work is absent. `c2Binding` stays null. Chazz/op044 must
identify the existing formed interfaces before a later integration; do not duplicate them.
`map_c2_circuit` was observed but is not assumed to be the c2ME_env contribution contract.

Remaining formation: define named individual/CI participation, 3-2-2 input fields,
3x3 mappings, c3 initiative schema, encounter copy and review disposition; map
exact existing database records and OAR2/CanCom references. No meaning for these
models is invented in this shell. Persisted c1/C1 closure and separately governed
integration remain required before any operational eligibility consumption.

Founding hard wall: c2 inherits eligibility to participate, not authority from c1.
